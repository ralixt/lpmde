{% extends 'base.html.twig' %}

{% block title %}Mon Profil — La Petite Maison de l'Épouvante{% endblock %}

{% block body %}
<div class="max-w-[1200px] mx-auto px-4 lg:px-10 py-20 space-y-12">

    <div class="flex flex-col sm:flex-row sm:items-end justify-between gap-6">
        <div class="space-y-3">
            <span class="inline-block px-3 py-1 bg-primary/20 text-primary border border-primary/30 text-[10px] font-bold uppercase tracking-widest rounded">
                Mon compte
            </span>
            <h1 class="text-5xl font-black uppercase tracking-tighter text-white leading-none">
                {{ user_name }}
            </h1>
            <p class="text-slate-500 text-sm uppercase tracking-widest">{{ user_email }}</p>
        </div>

        <a href="{{ path('app_logout') }}"
           class="inline-flex items-center gap-2 bg-primary/20 hover:bg-primary border border-primary/40 text-white font-bold py-3 px-6 rounded-lg transition-all uppercase text-xs tracking-widest group"
           data-tooltip="Se déconnecter">
            <span class="material-symbols-outlined text-sm" aria-hidden="true">logout</span>
            Déconnexion
        </a>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">

        <div class="lg:col-span-2 space-y-6">

            <section aria-labelledby="profile-info-title" class="bg-surface-dark border border-white/10 rounded-2xl overflow-hidden">
                <div class="flex items-center gap-4 p-6 border-b border-white/5">
                    <span class="material-symbols-outlined text-primary" aria-hidden="true">person</span>
                    <h2 id="profile-info-title" class="font-bold uppercase tracking-widest text-xs text-white">Informations du profil</h2>
                </div>
                <div class="p-6 space-y-4">
                    <div class="bg-white/5 border border-white/5 rounded-xl p-4">
                        <p class="text-[10px] font-bold uppercase tracking-widest text-slate-500 mb-1">Nom complet</p>
                        <p class="text-white font-semibold">{{ user_name }}</p>
                    </div>
                    <div class="bg-white/5 border border-white/5 rounded-xl p-4">
                        <p class="text-[10px] font-bold uppercase tracking-widest text-slate-500 mb-1">Adresse e-mail</p>
                        <p class="text-white font-semibold">{{ user_email }}</p>
                    </div>
                    <div class="flex items-center gap-3 bg-primary/10 border border-primary/20 rounded-xl p-4">
                        <span class="material-symbols-outlined text-primary" aria-hidden="true">verified</span>
                        <div>
                            <p class="font-bold text-sm text-white">Authentifié via Keycloak</p>
                            <p class="text-[10px] text-slate-500 uppercase tracking-widest">OAuth 2.0 / OpenID Connect</p>
                        </div>
                    </div>
                </div>
            </section>

            <section aria-labelledby="quick-actions-title" class="bg-surface-dark border border-white/10 rounded-2xl overflow-hidden">
                <div class="flex items-center gap-4 p-6 border-b border-white/5">
                    <span class="material-symbols-outlined text-primary" aria-hidden="true">bolt</span>
                    <h2 id="quick-actions-title" class="font-bold uppercase tracking-widest text-xs text-white">Actions rapides</h2>
                </div>
                <nav aria-label="Actions rapides" class="p-6 grid grid-cols-1 sm:grid-cols-2 gap-4">
                    <a href="{{ path('troc_index') }}"
                       class="flex items-center gap-3 bg-white/5 border border-white/10 hover:border-primary/50 rounded-xl p-4 transition-colors group"
                       data-tooltip="Voir toutes les annonces">
                        <span class="material-symbols-outlined text-slate-500 group-hover:text-primary transition-colors" aria-hidden="true">swap_horiz</span>
                        <div>
                            <p class="font-bold text-sm text-white">Espace Troc</p>
                            <p class="text-[10px] text-slate-500 uppercase tracking-widest">Toutes les annonces</p>
                        </div>
                    </a>
                    <a href="{{ path('troc_mes_annonces') }}"
                       class="flex items-center gap-3 bg-white/5 border border-white/10 hover:border-primary/50 rounded-xl p-4 transition-colors group"
                       data-tooltip="Gérer mes annonces">
                        <span class="material-symbols-outlined text-slate-500 group-hover:text-primary transition-colors" aria-hidden="true">list_alt</span>
                        <div>
                            <p class="font-bold text-sm text-white">Mes annonces</p>
                            <p class="text-[10px] text-slate-500 uppercase tracking-widest">Gérer mes offres</p>
                        </div>
                    </a>
                    <a href="{{ path('troc_new') }}"
                       class="flex items-center gap-3 bg-white/5 border border-white/10 hover:border-primary/50 rounded-xl p-4 transition-colors group"
                       data-tooltip="Créer une annonce">
                        <span class="material-symbols-outlined text-slate-500 group-hover:text-primary transition-colors" aria-hidden="true">add_circle</span>
                        <div>
                            <p class="font-bold text-sm text-white">Nouvelle annonce</p>
                            <p class="text-[10px] text-slate-500 uppercase tracking-widest">Publier un échange</p>
                        </div>
                    </a>
                    <a href="{{ path('homepage') }}"
                       class="flex items-center gap-3 bg-white/5 border border-white/10 hover:border-primary/50 rounded-xl p-4 transition-colors group"
                       data-tooltip="Retour à l'accueil">
                        <span class="material-symbols-outlined text-slate-500 group-hover:text-primary transition-colors" aria-hidden="true">home</span>
                        <div>
                            <p class="font-bold text-sm text-white">Accueil</p>
                            <p class="text-[10px] text-slate-500 uppercase tracking-widest">Retour au sanctuaire</p>
                        </div>
                    </a>
                </nav>
            </section>

        </div>

        <aside aria-label="Informations du compte" class="space-y-6">

            <div class="bg-surface-dark border border-white/10 rounded-2xl p-6 flex flex-col items-center gap-4 text-center">
                <div class="w-20 h-20 rounded-full bg-primary/20 border-2 border-primary/30 flex items-center justify-center" aria-hidden="true">
                    <span class="material-symbols-outlined text-primary" style="font-size: 2.5rem;" aria-hidden="true">account_circle</span>
                </div>
                <div>
                    <p class="font-black text-lg uppercase tracking-tighter text-white">{{ user_name }}</p>
                    <p class="text-[10px] text-primary uppercase tracking-widest font-bold">Membre actif</p>
                </div>
            </div>

            <section aria-labelledby="tech-stack-title" class="bg-surface-dark border border-white/10 rounded-2xl overflow-hidden">
                <div class="p-4 border-b border-white/5">
                    <h2 id="tech-stack-title" class="text-[10px] font-bold uppercase tracking-widest text-slate-500">Stack technique</h2>
                </div>
                <dl class="divide-y divide-white/5">
                    <div class="flex items-center gap-3 p-4">
                        <span class="material-symbols-outlined text-primary text-sm" aria-hidden="true">key</span>
                        <div>
                            <dt class="text-xs font-bold text-white">Keycloak</dt>
                            <dd class="text-[10px] text-slate-600 uppercase">Authentification</dd>
                        </div>
                        <span class="ml-auto text-[10px] text-primary font-bold uppercase tracking-widest">OAuth 2.0</span>
                    </div>
                    <div class="flex items-center gap-3 p-4">
                        <span class="material-symbols-outlined text-primary text-sm" aria-hidden="true">token</span>
                        <div>
                            <dt class="text-xs font-bold text-white">JWT Token</dt>
                            <dd class="text-[10px] text-slate-600 uppercase">Session</dd>
                        </div>
                        <span class="ml-auto text-[10px] text-primary font-bold uppercase tracking-widest">OIDC</span>
                    </div>
                    <div class="flex items-center gap-3 p-4">
                        <span class="material-symbols-outlined text-primary text-sm" aria-hidden="true">queue</span>
                        <div>
                            <dt class="text-xs font-bold text-white">RabbitMQ</dt>
                            <dd class="text-[10px] text-slate-600 uppercase">Messaging</dd>
                        </div>
                        <span class="ml-auto text-[10px] text-primary font-bold uppercase tracking-widest">AMQP</span>
                    </div>
                </dl>
            </section>

            <div class="flex items-start gap-3 bg-white/5 border border-white/10 rounded-xl p-4">
                <span class="material-symbols-outlined text-slate-500 text-sm mt-0.5" aria-hidden="true">info</span>
                <p class="text-xs text-slate-400 leading-relaxed">
                    Votre connexion a été enregistrée de manière asynchrone via RabbitMQ.
                </p>
            </div>

        </aside>
    </div>

</div>
{% endblock %}
